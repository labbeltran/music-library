import load_spinner from './components/loading-buffering.gif'

const Spinner = () => {
    return (
        <div>
            <img src={load_spinner} alt="loading..." />
        </div>
    )
}

export default Spinner